# Database
POSTGRES_USER=ntd_user
POSTGRES_PASSWORD=change_me
POSTGRES_DB=ntd_ridership
DATABASE_URL=postgresql://ntd_user:change_me@db:5432/ntd_ridership

# Backend — comma-separated list of allowed CORS origins
BACKEND_CORS_ORIGINS=["http://localhost:3000"]

# Frontend — URL of the backend API (must be reachable from the browser)
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
